import { createStyles } from '@material-ui/core';

const RdsTitleStyles = () =>
  createStyles({
    root: {
      fontWeight: 400
    },
    light: {
      fontWeight: 300
    }
  });

export default RdsTitleStyles;

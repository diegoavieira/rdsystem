import { createStyles } from '@material-ui/core';

const RdsImageStyles = () =>
  createStyles({
    root: {
      margin: 'auto 0',
      maxWidth: '100%',
      maxHeight: '100%',
      objectFit: 'cover'
    }
  });

export default RdsImageStyles;

import { createStyles } from '@material-ui/core';

const FrameStyles = () =>
  createStyles({
    root: {
      width: '100%',
      height: 400,
      border: 'none'
    }
  });

export default FrameStyles;
